{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!{% endblock %}

{% block body %}
    <div class="flex flex-col gap-5">
        {% for category in categories %}
            <a href="{{ path('app_category', {slug: category.slug}) }}">{{ category.title }}</a>
        {% endfor %}
    </div>
{% endblock %}
