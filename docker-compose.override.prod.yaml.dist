services:
  nginx:
    image: nginxproxy/nginx-proxy:1.7
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro

  web:
    environment:
      VIRTUAL_HOST: test.local
      VIRTUAL_PORT: 80